import { IconButton } from "@mui/material";
import React from "react";

interface M3Props {}
const M3IconButton = (props: M3Props) => {
  return <IconButton {...props} />;
};

export default M3IconButton;
